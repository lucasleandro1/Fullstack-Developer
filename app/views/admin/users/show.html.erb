<% content_for :title, "User Details - #{@user.display_name}" %>

<div class="container">
  <!-- Header -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1>User Details</h1>
      <p class="text-muted">View and manage user information</p>
    </div>
    <div class="d-flex gap-2">
      <%= link_to edit_admin_user_path(@user), class: "btn btn-primary" do %>
        <i class="bi bi-pencil me-2"></i>Edit User
      <% end %>
      <%= link_to admin_users_path, class: "btn btn-outline-secondary" do %>
        <i class="bi bi-arrow-left me-2"></i>Back to Users
      <% end %>
    </div>
  </div>

  <div class="row">
    <!-- User Profile Card -->
    <div class="col-lg-4">
      <div class="card user-card">
        <div class="card-body text-center">
          <% if @user.avatar %>
            <%= image_tag @user.avatar, class: "user-avatar mx-auto d-block mb-3", alt: @user.display_name %>
          <% else %>
            <div class="user-avatar mx-auto d-flex align-items-center justify-content-center bg-primary text-white mb-3">
              <span class="fw-bold" style="font-size: 2rem;"><%= @user.initials %></span>
            </div>
          <% end %>
          
          <div class="user-info">
            <h3 class="user-name"><%= @user.display_name %></h3>
            <p class="user-email"><%= @user.email %></p>
            <span class="user-role role-<%= @user.role %>">
              <% if @user.admin? %>
                <i class="bi bi-shield-check me-1"></i>Administrator
              <% else %>
                <i class="bi bi-person me-1"></i>Regular User
              <% end %>
            </span>
          </div>
        </div>
      </div>

      <!-- Quick Actions -->
      <div class="card mt-3">
        <div class="card-header">
          <h6 class="card-title mb-0">
            <i class="bi bi-lightning me-2"></i>Quick Actions
          </h6>
        </div>
        <div class="card-body">
          <div class="d-grid gap-2">
            <%= link_to edit_admin_user_path(@user), class: "btn btn-outline-primary" do %>
              <i class="bi bi-pencil me-2"></i>Edit Profile
            <% end %>
            
            <%= link_to toggle_role_admin_user_path(@user), 
                  method: :patch,
                  class: "btn btn-outline-warning #{'disabled' if @user == current_user}",
                  data: { confirm: "Are you sure you want to change this user's role?" } do %>
              <i class="bi bi-arrow-repeat me-2"></i>
              Toggle Role (<%= @user.admin? ? 'Make User' : 'Make Admin' %>)
            <% end %>
            
            <%= mail_to @user.email, class: "btn btn-outline-info" do %>
              <i class="bi bi-envelope me-2"></i>Send Email
            <% end %>
            
            <% unless @user == current_user %>
              <%= link_to admin_user_path(@user), 
                    method: :delete,
                    class: "btn btn-outline-danger",
                    data: { confirm: "Are you sure you want to delete this user? This action cannot be undone." } do %>
                <i class="bi bi-trash me-2"></i>Delete User
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <!-- User Details -->
    <div class="col-lg-8">
      <!-- Personal Information -->
      <div class="card">
        <div class="card-header">
          <h5 class="card-title mb-0">
            <i class="bi bi-person me-2"></i>Personal Information
          </h5>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Full Name</label>
                <p class="mb-0"><%= @user.full_name %></p>
              </div>
            </div>
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Email Address</label>
                <p class="mb-0">
                  <%= mail_to @user.email, @user.email, class: "text-decoration-none" %>
                </p>
              </div>
            </div>
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Role</label>
                <p class="mb-0">
                  <span class="role-badge role-<%= @user.role %>">
                    <%= @user.role.humanize %>
                  </span>
                </p>
              </div>
            </div>
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Member Since</label>
                <p class="mb-0">
                  <%= @user.created_at.strftime("%B %d, %Y") %>
                  <small class="text-muted">(<%= time_ago_in_words(@user.created_at) %> ago)</small>
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Account Information -->
      <div class="card mt-3">
        <div class="card-header">
          <h5 class="card-title mb-0">
            <i class="bi bi-shield-lock me-2"></i>Account Information
          </h5>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Last Sign In</label>
                <p class="mb-0">
                  <% if @user.current_sign_in_at %>
                    <%= @user.current_sign_in_at.strftime("%B %d, %Y at %I:%M %p") %>
                    <br><small class="text-muted"><%= time_ago_in_words(@user.current_sign_in_at) %> ago</small>
                  <% else %>
                    <span class="text-muted">Never signed in</span>
                  <% end %>
                </p>
              </div>
            </div>
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Sign In Count</label>
                <p class="mb-0">
                  <%= @user.sign_in_count || 0 %> times
                </p>
              </div>
            </div>
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Account Status</label>
                <p class="mb-0">
                  <% if @user.current_sign_in_at.present? && @user.current_sign_in_at > 30.days.ago %>
                    <span class="badge bg-success">Active</span>
                  <% elsif @user.current_sign_in_at.present? %>
                    <span class="badge bg-warning">Inactive</span>
                  <% else %>
                    <span class="badge bg-secondary">Never logged in</span>
                  <% end %>
                </p>
              </div>
            </div>
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label fw-bold">Profile Updated</label>
                <p class="mb-0">
                  <%= @user.updated_at.strftime("%B %d, %Y") %>
                  <small class="text-muted">(<%= time_ago_in_words(@user.updated_at) %> ago)</small>
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Activity Summary -->
      <% if @user.admin? %>
        <div class="card mt-3">
          <div class="card-header">
            <h5 class="card-title mb-0">
              <i class="bi bi-activity me-2"></i>Admin Activity
            </h5>
          </div>
          <div class="card-body">
            <div class="row text-center">
              <div class="col-md-4">
                <div class="border-end">
                  <h4 class="text-primary"><%= @user.imports.count %></h4>
                  <small class="text-muted">Imports Created</small>
                </div>
              </div>
              <div class="col-md-4">
                <div class="border-end">
                  <h4 class="text-success"><%= @user.imports.where(status: 'completed').count %></h4>
                  <small class="text-muted">Successful Imports</small>
                </div>
              </div>
              <div class="col-md-4">
                <h4 class="text-info">
                  <%= @user.imports.sum(:successful_rows) %>
                </h4>
                <small class="text-muted">Users Imported</small>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
