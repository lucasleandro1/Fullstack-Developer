<% content_for :title, "My Profile" %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <!-- Profile Header -->
      <div class="card user-card mb-4">
        <div class="card-body text-center">
          <div class="row align-items-center">
            <div class="col-md-4">
              <% if @user.avatar %>
                <%= image_tag @user.avatar, class: "user-avatar rounded-circle mx-auto d-block", alt: @user.display_name %>
              <% else %>
                <div class="user-avatar rounded-circle mx-auto d-flex align-items-center justify-content-center bg-primary text-white">
                  <i class="bi bi-person" style="font-size: 3rem;"></i>
                </div>
              <% end %>
            </div>
            <div class="col-md-8 text-md-start mt-3 mt-md-0">
              <div class="user-info">
                <h2 class="user-name"><%= @user.display_name %></h2>
                <p class="user-email"><%= @user.email %></p>
                <span class="user-role role-<%= @user.role %>">
                  <%= @user.role.humanize %>
                </span>
                <div class="mt-3">
                  <small class="text-muted">
                    <i class="bi bi-calendar me-2"></i>
                    Member since <%= @user.created_at.strftime("%B %Y") %>
                  </small>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Profile Actions -->
      <div class="card">
        <div class="card-header">
          <h5 class="card-title mb-0">
            <i class="bi bi-gear me-2"></i>Profile Settings
          </h5>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-6 mb-3">
              <h6>Personal Information</h6>
              <p class="text-muted mb-0">
                Update your personal details and avatar image.
              </p>
            </div>
            <div class="col-md-6 text-md-end">
              <%= link_to profile_edit_path, class: "btn btn-primary" do %>
                <i class="bi bi-pencil me-2"></i>Edit Profile
              <% end %>
            </div>
          </div>
          
          <hr>
          
          <div class="row">
            <div class="col-md-6 mb-3">
              <h6>Account Security</h6>
              <p class="text-muted mb-0">
                Change your password and manage account settings.
              </p>
            </div>
            <div class="col-md-6 text-md-end">
              <%= link_to edit_user_registration_path, class: "btn btn-outline-secondary" do %>
                <i class="bi bi-shield-lock me-2"></i>Security Settings
              <% end %>
            </div>
          </div>
          
          <hr>
          
          <div class="row">
            <div class="col-md-6 mb-3">
              <h6 class="text-danger">Danger Zone</h6>
              <p class="text-muted mb-0">
                Permanently delete your account and all data.
              </p>
            </div>
            <div class="col-md-6 text-md-end">
              <%= link_to profile_path, 
                    method: :delete,
                    class: "btn btn-outline-danger",
                    data: { 
                      confirm: "Are you sure you want to delete your account? This action cannot be undone." 
                    } do %>
                <i class="bi bi-trash me-2"></i>Delete Account
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
